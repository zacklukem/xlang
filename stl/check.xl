
fun panic_raw(str: *[]u8,line_col: *[]u8) {
    @c(var str: $str, var line_col: $line_col ; ; "
        __print_panic_message__($str, $line_col);
        exit(101);
    ");
}

fun assert_raw(expr: bool, str: *[]u8, line_col: *[]u8) {
    if !expr {
        panic_raw(str, line_col);
    }
}